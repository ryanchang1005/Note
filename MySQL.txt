登入
mysql --host=127.0.0.1 --port=3306 -u root -p database_name

匯入
mysql --host=127.0.0.1 --port=3306 -u root -p database_name < file.sql

顯示所有資料庫
SHOW DATABASES;

使用資料庫
USD database_name;

顯示所有表
SHOW TABLES;